import { BlogStructure } from "@/context/EditorContext";

const Previous = ({ title, banner, desc, content, state, tags }: BlogStructure) => {
  return (
    <div>Previous</div>
  )
}
export default Previous;